{
	"name": "tokenizart42",
	"version": "1.0.0",
	"description": "TokenizArt42 - ERC-721 NFT on Sepolia Testnet",
	"scripts": {
		"compile": "hardhat compile",
		"test": "hardhat test",
		"deploy": "hardhat run ../deployment/deploy.js --network sepolia",
		"mint": "hardhat run ../mint/mint.js --network sepolia"
	},
	"devDependencies": {
		"@nomicfoundation/hardhat-toolbox": "^4.0.0",
		"hardhat": "^2.19.0"
	},
	"dependencies": {
		"@openzeppelin/contracts": "^5.0.0",
		"dotenv": "^16.3.1"
	}
}
